<!DOCTYPE html>
<html lang="jp">

<head>
    <%= render :erb ,:'template/head' %>
</head>

<body>
    <%= render :erb ,:'template/header' %>
    <div class="uk-section" style="background: #eee;">
        <div class="uk-container uk-text-center">
            <h1 class="uk-heading-line"><span><%= @music.name %></span></h1>
            <span style="font-size: 18pt;"><%= @music.singers.map{|s|s.name}.join(',') %></span>
        </div>
    </div>
    <div id="music" class="uk-section">
        <div class="uk-container uk-text-center">
        
            <table>
                <tr>
                    <th>タイトル</th>
                    <th>投稿者</th>
                    <th>投稿日</th>
                    <th>色</th>
                </tr>
                <% @music.reviews.each do |r| %>
                <% twitter = Review.find(r.user.id) %>
                <tr>
                    <td><a href="/@<%= @twitter.screen_name %>/<%= r.id %>"><%= r.title %></a></td>
                    <td><a href="/@<%= @twitter.screen_name %>"><%= @twitter.name %>(@<%= @twitter.screen_name %>)</a></td>
                    <td><%= r.created_at %></td>
                    <td><span style="color: hsl(<%= r.color[1,100] %>, 100%, <%= r.color[0,1] == 'l' ? '85%' : '30%' %>,1)">■</span></td>

                </tr>
                <% end %>
            </table>
        </div>

    </div>
    <%= render :erb ,:'template/footer' %>
</body>

</html>